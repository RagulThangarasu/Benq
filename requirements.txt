# Web app
Flask>=2.0
Werkzeug>=2.0
gunicorn>=20.1
Python-dotenv>=0.19.0

# PDF reading, rendering and reporting
PyMuPDF>=1.20
reportlab>=3.6
Pillow>=10.0

# Artwork detection (figure regions) — imported by content_validation.dual_extract
# and used by the visual lane. Headless build: no GUI libraries, no X11.
opencv-python-headless>=4.8
numpy>=1.24

# Site validation lane only
playwright>=1.40

# Optional: LLM second-opinion pass that prunes extraction-artifact content
# findings. Skipped at runtime if unavailable or VALIDATOR_LLM_VERIFY=0.
anthropic>=0.40

# content_validation/ai_validate.py — standalone AI PDF-to-PDF content validator.
# No extra packages and no API keys: talks to a local Ollama over plain HTTP.
#   Setup:  ollama serve  +  ollama pull llama3.2:3b   (llama3.2:1b for max speed)
#   Run:    python -m content_validation.ai_validate prod.pdf stage.pdf -o reports/ai_validation_report.md
rapidfuzz>=3.0

# Unpacking the append-only prod-pdf's ZIPs: their per-product archives are .7z.
# Without this only the nested .zip products are extracted (.7z ones are skipped).
py7zr>=0.20
